/// This is a class where all endpoints are stored
/// You can then reference them in code with `AppEndpoints.someEndpoint`

class AppEndpoints {
  static const movieDbBase = 'https://api.themoviedb.org/3/';
}
